/**
 * RLM (Recurrent Language Model) module for LCF compaction
 * 
 * Exports types and the RLM engine for pattern-based summarization.
 */

export * from "./types.js";
export { RlmEngine, createRlmEngine } from "./rlm.js";

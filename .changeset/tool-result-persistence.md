---
"@martian-engineering/lossless-claw": minor
---

Add oversized `toolResult` offloading so large tool outputs can be persisted outside the main transcript with deterministic previews left behind for the active session.

This release also aligns the plugin with the current OpenClaw SDK surface, refreshes `dist/` before packaging, and documents the new persistence and release-check workflows.

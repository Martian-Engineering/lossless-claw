---
"@martian-engineering/lossless-claw": patch
---

Restore direct-credential summarizer retries for custom provider aliases and avoid misreporting transient provider failures as `provider_config` errors.

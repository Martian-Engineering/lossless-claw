---
"@martian-engineering/lossless-claw": patch
---

Add author-defined settings groups for OpenClaw hosts that support manifest `configGroups`. Older hosts retain the complete flat settings form; configuration paths, defaults, and runtime behavior are unchanged.

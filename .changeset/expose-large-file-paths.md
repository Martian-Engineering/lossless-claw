---
"@martian-engineering/lossless-claw": patch
---

Add an opt-in `exposeLargeFilePaths` setting that includes a validated local path in newly externalized large tool-output references while retaining LCM file-ID retrieval as a fallback.
